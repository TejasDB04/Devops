apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: readonly-role
  namespace: default
rules:
# Can only read pods, no delete/edit
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get", "list", "watch"]
# Note: Secrets are not listed, so users with this role cannot access them
