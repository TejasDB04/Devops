apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: secret-reader
  namespace: default
rules:
# Can only READ secrets (not delete/edit)
- apiGroups: [""]
  resources: ["secrets"]
  verbs: ["get", "list"]
